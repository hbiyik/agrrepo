# Maintainer: Mahmut Dikcizgi <boogiepop a~t gmx com>
#	https://download.acestream.media/android/media.web/stable/AceStreamMedia-3.1.80.0-armv8_64.apk
_srcname=docker-acestream-aceserve
_bin=acestreamengine
_pkgname=acestream-media
pkgname=$_pkgname-bin
pkgver=3.2.17
pkgrel=1
pkgdesc='Acestream Media Service'
arch=('aarch64' 'armv7h' 'x86_64')
url='https://acestream.org'
depends=(systemd)
provides=("acestream-engine")
conflicts=("acestream-engine")
makedepends=('unzip')
options=(!strip !debug)
source=("https://github.com/jopsis/$_srcname/archive/refs/tags/v$pkgver.tar.gz"
        $_bin
        service)
sha256sums=('2c654f246ba56a825669eabcfb74c4abfc8c293d102f9d143ce98c9f85601b40'
            '2ffbc92ff1da3f16f6b88bbf4a0642ad3a5c54d88a890c3d9fb09d79d8cdbdc5'
            'cc4578f068496025532bbff2ac5a4f2b07c6fab89a4fb96669322a11b3afa914')
install=$_pkgname.install

package() {
  _arch=$CARCH
  if [[ $CARCH == "armv7h" ]]; then
    _arch="arm32"
  fi
  if [[ $CARCH == "aarch64" ]]; then
    _arch="arm64"
  fi

  _prefix="/usr/lib/acestream"
  _statedir="/var/lib/ACEStream"
  install -d ${pkgdir}${_statedir}
  install -d ${pkgdir}${_prefix}/usr
  install -d ${pkgdir}${_prefix}/sys
  install -d ${pkgdir}${_prefix}/dev
  install -d ${pkgdir}${_prefix}/proc
  install -d ${pkgdir}${_prefix}/run

  unzip -o "${srcdir}/$_srcname-$pkgver/aceserve-$_arch/system.zip" -d ${pkgdir}${_prefix}
  unzip -o "${srcdir}/$_srcname-$pkgver/aceserve-$_arch/ace-*.zip" -d ${pkgdir}${_prefix}/acestream/
  chmod -R +x ${pkgdir}${_prefix}/system/bin
  chmod -R +x ${pkgdir}${_prefix}/acestream/python/bin
  cp -f ${srcdir}/$_srcname-$pkgver/aceserve-$_arch/main.py ${pkgdir}${_prefix}/acestream/
  cp -f ${srcdir}/$_srcname-$pkgver/aceserve-$_arch/dnsproxyd.py ${pkgdir}${_prefix}/acestream/
  cp -f ${srcdir}/$_srcname-$pkgver/aceserve-$_arch/app_bridge.py ${pkgdir}${_prefix}/acestream/
  
  install -Dm755 ${srcdir}/$_bin ${pkgdir}/usr/bin/$_bin
  install -Dm644 "service" "$pkgdir/usr/lib/systemd/system/$_pkgname.service"
}
