eval "$(curl -s -L https://raw.githubusercontent.com/hbiyik/agrrepo/refs/heads/master/libinherit/remote.sh)"
inherit "https://aur.archlinux.org/cgit/aur.git/plain" "?h=espeak-ng-git"

arch+=("aarch64" "armv7h")

check(){
  echo "skipping check"
}

