eval "$(curl -s -L https://raw.githubusercontent.com/hbiyik/agrrepo/refs/heads/master/libinherit/remote.sh)"
inherit "https://gitlab.archlinux.org/archlinux/packaging/packages/onednn/-/raw/main"

arch+=("aarch64" "armv7h")

check(){
 echo "skipping check"
}
